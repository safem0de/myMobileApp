import 'package:flutter/material.dart';

class InsertSlot extends StatelessWidget {
  const InsertSlot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Insert Slot'),
    );
  }
}
